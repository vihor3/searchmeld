# 复制为 .env 后修改以下必填项
# cp .env.example .env

# 对外端口（宿主机）
HOST_PORT=5173

# install.sh 管理的安装模式；一般不需要手工修改
SEARCHMELD_INSTALL_MODE=embedded
SEARCHMELD_USE_SHARED_DB_NETWORK=false
SEARCHMELD_USE_CUSTOM_DNS=false
SEARCHMELD_DNS_PRIMARY=
SEARCHMELD_DNS_SECONDARY=
# 从 One Search 升级时，旧版 ONE_SEARCH_* 安装配置仍会被识别。

# 容器默认时区；高级用户可改为 UTC 或其它有效的 IANA 时区
TZ=Asia/Shanghai

# 内置数据库密码（一体化模式必填；使用外部数据库时留空）
POSTGRES_PASSWORD=

# 外部 PostgreSQL（使用 docker-compose.external-db.yml 时填写）
# PostgreSQL 最低版本为 15；连接串中的特殊字符需进行 URL 编码
DATABASE_URL=
DATABASE_DOCKER_NETWORK=shared-db

# 管理员账号（首次启动创建）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# 管理台对外源；直接 HTTP 可留空，HTTPS 反代必须填写真实外部源
# 例如 https://search.example.com 或 https://search.example.com:8443
# 只填 http(s)://主机[:端口]，不要填写路径、查询参数或通配符
ADMIN_PUBLIC_ORIGIN=

# 加密密钥（必填，≥32 字符；丢失后无法解密已存 Key）
# 生成：openssl rand -base64 32
ENCRYPTION_KEY=

# 是否要求搜索 API 带 Token（默认 true）
API_AUTH_REQUIRED=true

# 是否开启 MCP（默认 true）
MCP_ENABLED=true
